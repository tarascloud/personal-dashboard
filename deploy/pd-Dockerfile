# Python scheduler for PD sync jobs (Garmin, Withings, Monobank, bunq)
FROM python:3.12-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc libpq-dev curl postgresql-client \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

RUN python -m compileall -q src/ deploy/ 2>/dev/null || true

RUN useradd --system --uid 1001 --no-create-home scheduler && \
    chown -R scheduler:scheduler /app

USER scheduler

CMD ["python", "deploy/scheduler.py"]
